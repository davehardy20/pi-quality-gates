{"id":"pl-dff0","seed":"pi-quality-gates-0d24","template":"refactor","status":"done","revision":1,"sections":{"execution_profile":{"mode":"orch-plan-run-lead"},"context":"src/linter/core.ts is a kitchen sink that loads config, runs markdownlint, runs CLI linters, discovers project roots, parses linter output, builds code excerpts, and merges outcomes. Its interface is nearly as large as its implementation, so callers and tests must reach into many exported helpers. This refactor introduces a LinterPipeline module with a small, deep interface and hides the linter adapters, config loading, report parsing, and outcome merging behind it.","behavior_invariant":"Given the same cwd, loaded LinterConfig, and set of modified file paths, the system must produce the same ValidationOutcome kind (clean/findings/tool-error), the same normalized affectedFiles list, the same reportMode, and the same bounded summary message content and sidecar metadata shape as before the refactor. The /post-turn-linter-run, /post-turn-linter-fix, and /post-turn-linter-report commands must continue to behave identically for users. Existing test assertions that are not tied to internal helper names must remain valid or be replaced by equivalent pipeline-seam tests.","approach":"Use a monolithic LinterPipeline with a public surface of runChecks(files) -> ValidationOutcome, summarize(outcome, reportId) -> summary, and persist(report) -> sidecar. Adapters are internal by default; a test-only constructor accepts fake adapters so tests drive the seam instead of calling __test__ internals. Split src/linter/core.ts into: src/linter/pipeline.ts (orchestrator), src/linter/config-loader.ts, src/linter/adapters/markdownlint.ts, src/linter/adapters/cli.ts, src/linter/adapters/lsp.ts (refactored from existing lsp.ts), src/linter/report-builder.ts (parse + excerpts), src/linter/outcome-merger.ts. Keep src/linter/index.ts as a thin extension orchestrator that owns state and commands. Rejected alternatives: (1) explicit dependency-injection pipeline — larger interface, leaks adapter composition; (2) three separate seams (runner/formatter/store) — more flexible but shallower and forces the orchestrator to know sequence; (3) adapter-by-adapter migration — rejected in favor of the full-pipeline PR Dave selected.","scope":"In scope: src/linter/core.ts split, src/linter/index.ts migration to the pipeline seam, src/linter/lsp.ts adapter refactor, src/linter/report-hygiene.ts summary builder kept as a pipeline consumer, and test/index.test.ts updated to use the seam. Out of scope: shared sidecar store with the reviewer (Candidate 2), reviewer context seams (Candidate 3), LSP client/server internals, adding new linter types, and README/feature changes beyond reflecting new module layout.","characterization_tests":"Before moving code, add tests in test/linter-pipeline.test.ts that exercise runQueuedLintChecks through the current public surface with in-memory file fixtures and stubbed spawn. Capture: markdownlint API path, CLI per-file grouping, project-root/workspace CLI modes, LSP merge behavior, and code excerpt generation. These tests pin behavior and must still pass after the refactor.","steps":[{"title":"Add characterization tests for current runQueuedLintChecks behavior","type":"task","priority":2,"labels":["tests","characterization"],"blocks":[2]},{"title":"Define LinterPipeline interface and move orchestration behind pipeline.ts","type":"task","priority":2,"labels":["architecture","pipeline"],"blocks":[3,4]},{"title":"Extract config loader into src/linter/config-loader.ts","type":"task","priority":2,"labels":["architecture","config"],"execution":{"mode":"parallel-subagents"},"blocks":[5]},{"title":"Extract linter adapters behind a LinterAdapter seam","type":"task","priority":2,"labels":["architecture","adapters"],"execution":{"mode":"parallel-subagents"},"blocks":[5]},{"title":"Extract report parser/excerpt builder and outcome merger","type":"task","priority":2,"labels":["architecture","reporting"],"blocks":[6]},{"title":"Migrate src/linter/index.ts to use LinterPipeline","type":"task","priority":2,"labels":["refactor","orchestration"],"blocks":[7]},{"title":"Update tests to exercise the pipeline seam with fake adapters","type":"task","priority":2,"labels":["tests","seam"],"blocks":[8]},{"title":"Run validation: typecheck, targeted tests, full suite","type":"task","priority":2,"labels":["validation"],"blocks":[9]},{"title":"Review pass and open PR","type":"task","priority":2,"labels":["review","pr"]}],"acceptance":["npm run typecheck passes with no errors","npm run test passes, including new characterization and pipeline-seam tests","Default config and CLI invocation paths produce the same ValidationOutcome shape as before","All /post-turn-linter-* commands remain functional in a smoke check","No new dependencies added unless required by the new module split","A feature branch is pushed and a PR is opened against the default branch"],"review_pass":"One read-only review focused on: whether the new pipeline interface is smaller and deeper, whether adapters are real (markdownlint/cli/lsp + test fake), whether any leaked implementation details remain in src/linter/index.ts, and whether tests cross the same seam as callers. No reviewer edits until findings are returned.","branch_pr":"Create a feature branch for this refactor (e.g. refactor/linter-pipeline). Commit intended changes, push, and open a PR. Do not merge; the PR is ready for review.","outcome_criteria":"Seeds plan outcome will be recorded as success when the PR is open, validation passes, and the pipeline seam is the only path used by src/linter/index.ts.","mulch_criteria":"After success, record a Mulch convention: when a module grows beyond one clear responsibility, prefer a single deep pipeline seam with internal adapters over many exported helpers, and keep tests on the same seam as callers."},"children":["pi-quality-gates-deab","pi-quality-gates-238b","pi-quality-gates-6fa1","pi-quality-gates-c692","pi-quality-gates-8c13","pi-quality-gates-f377","pi-quality-gates-5fa0","pi-quality-gates-09c7","pi-quality-gates-54a5"],"createdAt":"2026-06-14T21:01:40.238Z","updatedAt":"2026-06-14T21:39:41.899Z","name":"Deepen linter run-checks pipeline","outcome":"success","outcomeNote":"Implemented LinterPipeline seam, migrated src/linter/index.ts, extracted adapters/config/merger/report modules, deleted obsolete lsp.ts, updated tests, and opened PR #2. TypeScript, Biome, and Vitest all pass."}
{"id":"pl-568b","seed":"pi-quality-gates-b212","template":"refactor","status":"done","revision":1,"sections":{"execution_profile":{"mode":"orch-plan-run-lead"},"context":"The post-turn-linter and post-turn-reviewer both persist redacted reports to sidecar files and recover them via preview/slice/full modes. Today that logic is duplicated across src/linter/report-hygiene.ts (~631 lines) and src/reviewer/report-hygiene.ts (~343 lines). Redaction rules, path sanitization, SHA-256/byte accounting, temp-file writes, and recovery policy are implemented twice, so any security or storage fix has to be made in two places. This refactor moves the shared storage contract into one module and leaves each subsystem responsible only for its summary formatting and command argument parsing.","behavior_invariant":"After the refactor: (1) sidecar files are written atomically via temp-file + rename to the same ~/.pi/agent/tool-output/<sessionId>/<uuid>.json path; (2) persisted JSON contains the same metadata fields including id, toolName, sessionId, path, createdAt, originalChars, originalBytes, redactedChars, redactedBytes, originalSha256, redactedSha256, summaryMode, and optional failureState; (3) redaction removes the same secret patterns (api keys, bearer tokens, AWS keys, JWTs); (4) preview, slice, metadata, and full recovery modes behave identically, including the requirement that full recovery requires --ack-context-cost unless allowFullWithoutAck is true; (5) the default sidecar directory derivation and session-id derivation remain unchanged; (6) existing callers importing writeLinterReportSidecar / recoverLinterReportSidecar / writeReviewerReportSidecar / recoverReviewerReportSidecar continue to compile and behave the same.","approach":"Introduce a minimal generic core at src/shared/report-sidecar.ts that exposes writeReportSidecar<M extends ReportSidecarMetadata> and recoverReportSidecar<M extends ReportSidecarMetadata>. Callers pass toolName and summaryMode per invocation, so the core stays stateless and the existing linter/reviewer metadata types remain typed. Move isQualityGatesSubAgentRuntime to a tiny src/shared/runtime-detection.ts because it is currently duplicated and is used to decide recovery policy. Reduce src/linter/report-hygiene.ts and src/reviewer/report-hygiene.ts to summary formatting, argument parsing, and thin re-exports/wrappers that delegate storage to the shared module. This follows the existing Mulch pattern of preferring a deep seam (generic sidecar storage) over shallow helper modules and keeping adapter identity and file routing explicit.","steps":[{"title":"Add characterization tests for current sidecar behavior","type":"task","priority":1,"blocks":[2,3],"labels":["testing","sidecar"]},{"title":"Create src/shared/report-sidecar.ts generic storage module","type":"task","priority":1,"blocks":[4],"labels":["architecture","sidecar"]},{"title":"Create src/shared/runtime-detection.ts for sub-agent detection","type":"task","priority":2,"blocks":[4],"labels":["architecture","runtime"]},{"title":"Refactor src/linter/report-hygiene.ts to delegate to shared module","type":"task","priority":1,"blocks":[5,6],"labels":["refactor","linter"]},{"title":"Refactor src/reviewer/report-hygiene.ts to delegate to shared module","type":"task","priority":1,"blocks":[6],"labels":["refactor","reviewer"]},{"title":"Update callers and remove duplicated helpers","type":"task","priority":2,"blocks":[7],"labels":["refactor","cleanup"]},{"title":"Run validation and review pass","type":"task","priority":1,"blocks":[8],"labels":["validation","review"]},{"title":"Open PR and record Seeds outcome","type":"task","priority":1,"labels":["pr","closeout"]}],"acceptance":["npm run typecheck passes with no new errors.","npm run test passes, including existing linter and reviewer sidecar tests.","New characterization tests in test/sidecar.test.ts (or equivalent) verify write/recover/redaction for both linter and reviewer metadata shapes.","No behavior change in sidecar file path, metadata fields, recovery modes, or ack policy.","src/linter/report-hygiene.ts and src/reviewer/report-hygiene.ts no longer duplicate storage/redaction/path helpers.","A review pass confirms no runtime detection or storage policy remains duplicated.","All intended changes are committed on a feature branch and pushed; a PR is opened against the default branch."]},"children":["pi-quality-gates-add9","pi-quality-gates-c893","pi-quality-gates-1399","pi-quality-gates-b44c","pi-quality-gates-f44c","pi-quality-gates-1526","pi-quality-gates-1f16","pi-quality-gates-6e4a"],"createdAt":"2026-06-15T08:53:02.793Z","updatedAt":"2026-06-15T09:15:00.967Z","name":"Unify report sidecar module","outcome":"success","outcomeNote":"Shared report-sidecar seam extracted; linter/reviewer wrappers delegate storage/redaction/recovery. All tests, typecheck, and build pass. PR #3 opened."}
{"id":"pl-f57f","seed":"pi-quality-gates-3e76","template":"refactor","status":"done","revision":1,"sections":{"execution_profile":{"mode":"orch-plan-run-lead"},"context":"src/reviewer/index.ts is an 841-line extension entry point that combines Pi lifecycle hooks, a state machine, command registration, report formatting, reviewer sidecar persistence, and fix-request scheduling. After the LinterPipeline refactor, the linter extension has a clear run-checks seam, but the reviewer extension still exposes its internal state shape and transition rules to callers and tests. This refactor introduces a ReviewerOrchestrator module with a small, event-driven interface and hides the state machine, formatting, sidecar writes, and scheduling behind it.","behavior_invariant":"Given the same session events and linter status messages, the reviewer must produce the same phase transitions, the same custom_message outputs (advisory, findings, escalation, timeout, parse-fail, report), the same fix-up user messages, the same sidecar metadata shape, and the same /reviewer-* command behavior as before the refactor. Default config and CLI invocation paths must remain unchanged. Existing tests that assert behavior (not internal helper names) must stay valid or be replaced by equivalent orchestrator-seam tests.","approach":"Introduce src/reviewer/orchestrator.ts with an event-driven interface: initialize(ctx), onLinterClean(files, timestamp?), onTurnEnd(ctx), requestReview(ctx, opts?), registerCommands(pi), and getStateSnapshot(). The orchestrator owns the state machine, config/skip loading, report formatting, sidecar persistence, and fix-request scheduling. src/reviewer/index.ts becomes thin Pi wiring that delegates events and commands to the orchestrator. Tests drive the seam by injecting fake runReview, writeSidecar, loadConfig, and loadSkipFilter dependencies. Rejected alternatives: (1) explicit lifecycle + command dispatch — more uniform but forces the extension to map every Pi command name; (2) dependency-injected controller returning command handlers — slightly larger interface and leaks command wiring details; (3) also deepening the reviewer core pipeline in the same PR — too large; kept as a follow-up candidate.","scope":"In scope: splitting state machine + lifecycle logic out of src/reviewer/index.ts into src/reviewer/orchestrator.ts, defining a small orchestrator interface, migrating Pi event/command wiring, adding characterization tests, updating tests to use the new seam, and removing now-redundant __test__ exports from src/reviewer/index.ts. Out of scope: refactoring src/reviewer/reviewer.ts into a ReviewPipeline (Candidate 3), linter controller refactor (Candidate 2), shared change-detection module (Candidate 4), new reviewer domains or prompts, and changes to the shared sidecar store.","steps":[{"title":"Add characterization tests for current reviewer turn_end and command behavior","type":"task","priority":2,"labels":["testing","characterization"],"blocks":[3],"execution":{"mode":"parallel-subagents"}},{"title":"Define ReviewerOrchestrator interface and state snapshot types","type":"task","priority":2,"labels":["design","reviewer"],"blocks":[3],"execution":{"mode":"parallel-subagents"}},{"title":"Implement ReviewerOrchestrator behind the new seam","type":"task","priority":2,"labels":["implementation","reviewer"],"blocks":[4]},{"title":"Migrate src/reviewer/index.ts to thin wiring using the orchestrator","type":"task","priority":2,"labels":["refactoring","reviewer"],"blocks":[5,6]},{"title":"Update tests to exercise the orchestrator seam with fake dependencies","type":"task","priority":2,"labels":["testing","reviewer"],"blocks":[7]},{"title":"Remove now-redundant __test__ exports from src/reviewer/index.ts","type":"task","priority":2,"labels":["cleanup","reviewer"],"blocks":[7]},{"title":"Run validation: typecheck, targeted tests, full suite","type":"task","priority":2,"labels":["validation"],"blocks":[8]},{"title":"Review pass and open PR","type":"task","priority":2,"labels":["review","pr"]}],"acceptance":["npm run typecheck passes with no errors","npm run test passes, including new characterization and orchestrator-seam tests","Default config and /reviewer-* command behavior is unchanged","src/reviewer/index.ts is thin Pi wiring and does not contain state-machine logic","A feature branch is pushed and a PR is opened against the default branch"],"review_pass":"One read-only review focused on: whether the orchestrator interface is smaller and deeper than the current extension entry point, whether tests cross the same seam as callers, whether any state-machine logic leaked back into src/reviewer/index.ts, and whether the behavior invariant is covered by tests. No reviewer edits until findings are returned.","branch_pr":"Create a feature branch for this refactor (e.g. refactor/reviewer-orchestrator). Commit intended changes, push, and open a PR. Do not merge; the PR is ready for review.","outcome_criteria":"Seeds plan outcome will be recorded as success when the PR is open, validation passes, and src/reviewer/index.ts uses the orchestrator seam for all event and command handling.","mulch_criteria":"After success, record a Mulch convention: when an extension entry point grows beyond lifecycle wiring, prefer a single deep orchestrator seam with event-driven methods and injected dependencies over mixing state machine and Pi API calls in the entry file."},"children":["pi-quality-gates-7cd0","pi-quality-gates-98cb","pi-quality-gates-fd9e","pi-quality-gates-490f","pi-quality-gates-8e17","pi-quality-gates-8224","pi-quality-gates-f985","pi-quality-gates-c0fd"],"createdAt":"2026-06-15T10:39:50.788Z","updatedAt":"2026-06-15T11:11:13.050Z","name":"Deepen reviewer extension entry point","outcome":"success","outcomeNote":"Implemented ReviewerOrchestrator seam, migrated src/reviewer/index.ts to thin wiring, added orchestrator tests, completed self-review pass, and opened PR #4. All validation passes."}
{"id":"pl-6d3c","seed":"pi-quality-gates-6ae4","template":"refactor","status":"done","revision":1,"sections":{"execution_profile":{"mode":"orch-plan-run-lead","parallel_subagents":"allowed for independent subtasks only; initial dependency graph is mostly sequential"},"context":"src/reviewer/reviewer.ts is still a shallow toolbox module after the ReviewerOrchestrator refactor. It exposes gatherDiff, applyDiffFilters, renderTaskTemplate, spawnReviewer, parseReviewReport, hasFindingsAboveThreshold, and display helpers, so callers must know the execution order and failure modes for diff gathering, prompt rendering, child Pi execution, output parsing, and normalized review result handling. The selected candidate deepens this into a reviewer execution seam consumed by the orchestrator, while preserving the orchestrator as owner of session state, re-review loops, notifications, sidecar writes, and fix-turn scheduling.","behavior_invariant":"Given the same task, files, cwd, ReviewConfig, skip/gitignore filter options, prompts, child process output, stderr, exit code, timeout status, and abort signal, the refactored reviewer execution path must return the same ReviewerResult shape, parsed ReviewReport, rawOutput/stderr handling, sanitized command string, timeout behavior, diff truncation behavior, prompt rendering content, and file filtering behavior as the current helper sequence. Existing /reviewer-* command behavior, ReviewerOrchestrator phase transitions, sidecar metadata shape, parse-fail notices, timeout notices, advisory/fix/escalation messages, and default config behavior must remain unchanged.","approach":"Use the selected minimal reviewer execution seam: ReviewerExecution.runAttempt(input) -> Promise<ReviewerResult>, where input contains task, files, cwd, ReviewConfig, DiffFilterOptions, and optional AbortSignal. This follows the repo pattern of a deep pipeline/orchestrator seam with implementation details hidden behind it. The execution module owns context gathering for one review attempt: apply diff filters, gather/cap diff, read/render prompts, spawn the child Pi reviewer, parse output, and return the normalized ReviewerResult. src/reviewer/orchestrator.ts keeps the state machine and calls the seam; src/reviewer/index.ts only wires the default execution adapter. Rejected alternatives: (1) Pipeline object with gather/render/execute methods, rejected because it still exposes sequencing to callers; (2) explicit ports-and-adapters interface, rejected as over-designed until there are multiple real diff/prompt/child adapters; (3) changing prompt content or review domains, rejected as out of scope.","chosen_seam":"ReviewerExecution.runAttempt(input) -> ReviewerResult. The seam lives near src/reviewer/reviewer.ts or a new src/reviewer/execution.ts. It represents one review attempt, not the whole reviewer state machine.","scope":"In scope: characterization tests for current reviewer execution behavior, a reviewer execution interface/type, implementation of runAttempt using the existing helper logic, migration of ReviewerOrchestrator/default wiring to call the seam, tests that cross the same seam as callers, and cleanup of now-redundant public helper exports only when safe. Out of scope: prompt content changes, review domain changes, sidecar storage format changes, linter controller refactor, LSP diagnostics refactor, and changes to re-review policy beyond preserving existing behavior.","steps":[{"title":"Add characterization tests for current reviewer execution behavior","type":"task","priority":2,"labels":["tests","characterization"],"blocks":[2,3]},{"title":"Define ReviewerExecution seam and attempt input types","type":"task","priority":2,"labels":["architecture","interface"],"blocks":[3,4]},{"title":"Implement runAttempt behind the reviewer execution seam","type":"task","priority":2,"labels":["implementation","reviewer"],"blocks":[4,5]},{"title":"Migrate ReviewerOrchestrator and entry wiring to the execution seam","type":"task","priority":2,"labels":["migration","reviewer"],"blocks":[5,6]},{"title":"Update tests to cross the reviewer execution seam","type":"task","priority":2,"labels":["tests","migration"],"blocks":[6]},{"title":"Clean up redundant reviewer helper exports where safe","type":"task","priority":3,"labels":["cleanup"],"blocks":[7]},{"title":"Run validation: typecheck, targeted tests, full suite","type":"task","priority":2,"labels":["validation"],"blocks":[8]},{"title":"Review pass, open PR, and record Seeds outcome","type":"task","priority":2,"labels":["review","pr"],"execution":{"mode":"parallel-subagents","when":"reviewer may run read-only review while final PR notes are prepared after validation"}}],"acceptance":["npm run typecheck passes with no errors","npm run test passes, including characterization and execution-seam tests","ReviewerOrchestrator uses the reviewer execution seam for review attempts instead of assembling diff/prompt/spawn steps directly","Default /reviewer-* command behavior and post-turn reviewer messages remain unchanged","Timeout, parse-fail, sidecar, sanitized-command, diff truncation, and skip/gitignore filtering behavior are preserved","No prompt content or review domain changes are introduced","A feature branch is pushed and a PR is opened against the default branch"],"review_pass":"One read-only review focused on whether the execution seam is smaller and deeper than the old helper toolbox, whether the orchestrator still owns only state/notifications/loop policy, whether tests cross the same seam as callers, and whether any prompt/diff/spawn/parse sequencing leaks back into src/reviewer/index.ts or src/reviewer/orchestrator.ts.","branch_pr":"Work is already on feature branch architecture/reviewer-execution-seam. Commit intended changes, push the branch, and open a PR. Do not merge; the PR is ready for review.","outcome_criteria":"Record the Seeds plan outcome as success when validation passes, review is complete, the PR is open, and the reviewer execution seam is the path used by ReviewerOrchestrator for review attempts.","mulch_criteria":"After successful validation and review, record Mulch only if there is durable learning, likely: reviewer execution should be represented as one review-attempt seam while the orchestrator remains responsible for session state and fix/re-review policy. Do not record Mulch before outcome confirmation."},"children":["pi-quality-gates-97f3","pi-quality-gates-69b8","pi-quality-gates-8a39","pi-quality-gates-2e24","pi-quality-gates-d1e0","pi-quality-gates-f925","pi-quality-gates-af9a","pi-quality-gates-518d"],"createdAt":"2026-06-15T11:56:06.468Z","updatedAt":"2026-06-15T12:09:01.276Z","name":"Deepen reviewer execution seam","outcome":"success","outcomeNote":"Implemented ReviewerExecution.runAttempt seam, migrated ReviewerOrchestrator and entry wiring, updated tests to cross the seam, completed read-only review pass, validation passed (biome, typecheck, build typecheck, full vitest), and opened PR #5."}
